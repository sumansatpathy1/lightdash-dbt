version: 2

sources:
  - name: data_assurance
    tables:
      - name: reports
      - name: data_assurance_metrics
        columns:
            - name: id 
              tests:
                - unique
                - not_null
metrics: 
  - name: max_count
    label: metrics count
    model: ref('metrics')
    description: "something "
    type: count_distinct
    sql: id 
    dimensions:
      - plan 
      - country
